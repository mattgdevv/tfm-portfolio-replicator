# Portfolio Replicator TFM - Requirements
# Trabajo Final de Máster - Data Engineer

# Core Application Dependencies
requests>=2.32.0
pandas>=2.3.0
pydantic>=2.11.0
pydantic-settings>=2.10.0
python-dotenv>=1.1.0
urllib3>=2.5.0

# Excel/CSV Processing
openpyxl>=3.1.0

# PDF Processing (for BYMA data extraction)
PyPDF2>=3.0.0
pdfplumber>=0.11.0
beautifulsoup4>=4.13.0

# Date/Time utilities
holidays>=0.80

# Type hints (Python 3.8+ compatibility)
typing_extensions>=4.15.0

# Optional dependencies (used conditionally)
# psutil>=5.9.0  # Only used in monitoring commands if available
# tkinter is built-in to Python (for file dialogs)

# Development/Testing (commented out for production)
# loguru>=0.7.0  # Alternative logging (not required)
# tqdm>=4.67.0   # Progress bars (not required)
